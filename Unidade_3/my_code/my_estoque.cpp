#include <iostream>
#include <string>
#include <vector>

using namespace std;

template <typename T>
class produto{
    string nome;
    T descricao;
    int num_serie;
    float preco;
    int qtd;

public:
    produto(const string& no, T desc, int ns, float p, int q)
};