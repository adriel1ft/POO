#include <iostream>
#include <string>
#include <map>

using std::string;
using std::cout;
using std::cin;
using std::endl;
using std::map;

class student{
    string name, id;
    int age;

public:
student(const string& n, int a, const string& id): name(n), age(a), id(id){}

//gets e sets



};

class database{
    map<string, student*> students;

public:

};